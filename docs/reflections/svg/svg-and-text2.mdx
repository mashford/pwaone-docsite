---
sidebar_position: 2
---

# Svg and Texts

Here is an example how we can use monospace fonts to calculate the width of the text. The width of the text is calculated by the following formula:

width = (font-size*0.6)px * number of characters + 0.015625px

```jsx
import React from "react";
import "./style.css";

export default function App() {
  React.useEffect(() => {
    const element = document.getElementById("text");
    const cssObj = window.getComputedStyle(element, null);
    console.log(cssObj.getPropertyValue("width"));
    console.log(cssObj.getPropertyValue("font-family"));
    console.log(element.getBoundingClientRect().width);
    // width = 18px * number of characters + 0.015625px
  });
  return (
    <div>
      <svg width="150" height="100" xmlns="http://www.w3.org/2000/svg">
        <rect width="100%" height="100%" fill="green" />
        <circle cx="75" cy="50" r="40" fill="yellow" />
        <text
          id="text"
          x="75"
          y="60"
          fontSize="30"
          fontFamily="'Courier New', monospace"
          textAnchor="middle"
          fill="red"
        >
          12345
        </text>
      </svg>
    </div>
  );
}
```

(link)[https://stackblitz.com/edit/react-ocr4kn?file=src%2FApp.js,src%2Findex.js]
